<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >



    <android.support.v4.view.ViewPager
        android:id="@+id/vp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/rg"
        />


    <RadioGroup
        android:id="@+id/rg"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:background="@color/dhlqianhui"
        android:gravity="center"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/today_tab"
            style="@style/Coffeprogie"
            android:background="#00000000"
            android:button="@null"
            android:checked="true"
            android:drawableTop="@drawable/coffeezhuyeqietu"
            android:text="主页" />
        <RadioButton
            android:id="@+id/record_tab"
            style="@style/Coffeprogie"
            android:background="#00000000"
            android:button="@null"
            android:drawableTop="@drawable/coffeequan"
            android:text="圈"
            />

        <RadioButton
            android:id="@+id/tabs_rg"
            style="@style/Coffeprogie"
            android:background="#00000000"
            android:button="@null"
            android:drawableTop="@mipmap/coffeefabu"
            android:textSize="0dp"
            />

        <RadioButton
            android:id="@+id/contact_tab"
            style="@style/Coffeprogie"
            android:background="#00000000"
            android:button="@null"
            android:drawableTop="@drawable/coffeeshipin"
            android:text="视频" />

        <RadioButton
            android:id="@+id/settings_tab"
            style="@style/Coffeprogie"
            android:background="#00000000"
            android:button="@null"
            android:drawableTop="@drawable/coffeemys"
             android:text="我的" />
    </RadioGroup>

</RelativeLayout>